<%= form_with(model: courier_status) do |form| %>
  <% if courier_status.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(courier_status.errors.count, "error") %> prohibited this courier_status from being saved:</h2>

      <ul>
        <% courier_status.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
